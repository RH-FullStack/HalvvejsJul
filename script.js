document.addEventListener("DOMContentLoaded", function() {
    const forecastElement = document.getElementById('weather-forecast');

    // Simuler en prognose for sne
    const snowForecast = "Desværre, ingen sne i dag. Nyd sommeren!";

    forecastElement.textContent = snowForecast;
});
